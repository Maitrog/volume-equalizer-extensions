{
  "dependencies": {
    "chrome-types": "^0.1.349"
  },
  "scripts": {
    "prezip": "@powershell if (-not (Test-Path -Path \"./build/Release\")) {New-Item -ItemType Directory -Path \"./build/Release\"}",
    "zip": "bestzip ./build/Release/eq-toolkit_v1.4.2_unpackeged.zip resources/** scripts/** _locales/** manifest.json popup.html"
  },
  "devDependencies": {
    "bestzip": "^2.2.1"
  }
}